# Dependencies & build
node_modules/
dist/

# Secrets & môi trường local — không commit (pattern .env.* gồm cả .env.example)
.env
.env.*

# Logs
*.log
logs/

# Dữ liệu tải về / sinh ra khi chạy — không đưa lên GitHub
output/

# Excel chứa mật khẩu NPC trong data/ (giữ README, không commit .xlsx)
data/**/*.xlsx

# IDE / Cursor (máy local)
.cursor/
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# MCP descriptors local (Cursor project)
mcps/

# File tạm / script debug local
tmp-*.mjs

# Test / coverage
coverage/
playwright-report/
test-results/

# Tài liệu nội bộ — mặc định không track; ngoại lệ dưới đây đưa lên repo
docs_autocheckenv/**
!docs_autocheckenv/HANOI_AGENT_API.md
!docs_autocheckenv/AGENT_GATEWAY_HANOI_NOTES.md
!docs_autocheckenv/openapi.yaml
TESTING.md

# File nhạy cảm tạm thời (không commit)
npc-encrypted.json
npc-encrypted-decrypted.xlsx

# OS
ehthumbs.db
Desktop.ini
